# Pinned to the versions this site was built and verified against.
# Bump deliberately, then re-run `mkdocs build --strict` before pushing —
# the weekly cron in .github/workflows/render_page.yml rebuilds unattended.
setuptools==84.0.0
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-bibtex==4.4.0
neoteroi-mkdocs==1.2.0
mkdocs-video==1.5.0
addbioschemas==1.0.3
